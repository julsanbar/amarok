import { HabilitadoPipe } from './habilitado.pipe';

describe('HabilitadoPipe', () => {
  it('create an instance', () => {
    const pipe = new HabilitadoPipe();
    expect(pipe).toBeTruthy();
  });
});
